Turn the author's reply already saved in this workspace into citable
evidence, following the author-feedback loop in your instructions exactly.

- The manifest is checkpointed and every claim in it is capped at `gap` or
  `inferred`. The reply is saved verbatim, dated and attributed, at
  `interviews/int-001.md`; it was given before any question existed, so its
  sections are keyed by claim id. Never edit the transcript.
- Draft exactly one question for each claim the reply addresses, quoting
  that claim's text verbatim inside the question, then export the batch.
- Import the reply: record each answer against its question with a verbatim
  quote from the transcript, which attaches the interview anchor. Record a
  sign-off ONLY where the author confirmed the claim's exact wording; a
  paraphrase or a correction earns the anchor and never the sign-off.
- Record the newly supported statuses and re-run the strict gate. Never ask
  the user anything; there is no user in this session. If an operation is
  denied, do not retry it in another form — note the denial in your final
  summary and continue with what is permitted.
- End with a summary: questions drafted, answers recorded, sign-offs
  recorded, sign-offs withheld with the reason for each, gate status, and
  every denied or failed operation.
